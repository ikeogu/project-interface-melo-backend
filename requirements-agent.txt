# Requirements for the LiveKit voice agent worker (agent_worker.py).
# Installed in the Railway "agent" service — separate from the FastAPI app
# to avoid the openai 1.x vs 2.x conflict.
#
# All versions are pinned to what pip resolved via dry-run to prevent
# backtracking timeouts on Railway builds.

# LiveKit core (same as main app)
livekit==1.1.5
livekit-api==1.1.0
livekit-protocol==1.1.5

# LiveKit Agents + plugins (1.x requires openai>=2)
livekit-agents==1.5.6
livekit-plugins-openai==1.5.6
livekit-plugins-elevenlabs==1.5.6
livekit-plugins-silero==1.5.6
livekit-blingfire==1.1.0

# openai v2 (required by livekit-agents 1.x)
openai==2.32.0

# Agent pipeline dependencies (pinned from dry-run resolution)
av==17.0.1
colorama==0.4.6
docstring_parser==0.18.0
eval_type_backport==0.3.1
flatbuffers==25.12.19
googleapis-common-protos==1.74.0
grpcio==1.80.0
importlib_metadata==8.7.1
nest-asyncio==1.6.0
onnxruntime==1.25.0
opentelemetry-api==1.39.1
opentelemetry-exporter-otlp==1.39.1
opentelemetry-exporter-otlp-proto-common==1.39.1
opentelemetry-exporter-otlp-proto-grpc==1.39.1
opentelemetry-exporter-otlp-proto-http==1.39.1
opentelemetry-proto==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-semantic-conventions==0.60b1
pillow==12.2.0
prometheus_client==0.25.0
psutil==7.2.2
sounddevice==0.5.5
zipp==3.23.1

# Shared utilities
httpx==0.27.2
python-dotenv==1.0.1
anyio==4.13.0
certifi==2026.2.25
